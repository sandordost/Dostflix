import QtQuick
import QtQuick.Controls
import Dostflix

Item {
    Label {
        anchors.centerIn: parent
        text: qsTr("Settings arrive in the VPN phase")
        color: Theme.textSecondary
        font.pixelSize: Theme.bodySize
    }
}
